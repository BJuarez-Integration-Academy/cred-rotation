name: 'Execute Terraform'

on: [push, pull_request, workflow_dispatch]

env:
  TF_LOG: INFO
  passphraseFile: ${{ secrets.FILE_PASS_PHRASE }}
  GITHUB_OWNER: ${{ secrets.GIT_OWNER }}
  GITHUB_TOKEN: ${{ secrets.GIT_PAT }}

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest

    # Set the working directory to main for the config files
    defaults:
      run:
        shell: bash
        working-directory: ./main

    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Checkout
      uses: actions/checkout@v3

    - name: Decrypt and read file
      run: gpg --quiet --batch --yes --decrypt --passphrase="$passphraseFile" --output $HOME/terraformValues.tfvars terraformValues.tfvars.gpg

    # Install Terraform CLI 
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2

    # Terraform init
    - name: Terraform Init
      id: init
      run: terraform init

      #Terraform Validate
    - name: Terraform Validate
      id: validate
      run: terraform validate -no-color

    # Run a terraform plan
    - name: Terraform Plan
      id: plan
      run: terraform plan -var-file=$HOME/terraformValues.tfvars -no-color
    
    # Terraform Apply
    - name: Terraform Apply
      run: terraform apply -var-file=$HOME/terraformValues.tfvars -auto-approve
